import 'dart:ui';

import 'font_size.dart';

///TODO: we will be using typography for text like
///Heading/Titles, Sub-heading, label, normal text, etc which we do not have now
///Example:
/// TextStyle headingStyle = TextStyle(fontSize: FontSize.xxl, fontWeight: FontWeight.w700);
/// TextStyle subHeadingStyle = TextStyle(fontSize: FontSize.l, fontWeight: FontWeight.w500);